import React from 'react';
import classes from './Tag.module.css';

const Tag = props => {
    console.log(props);
    return (
        <div className={classes.wrapper}>
            
        </div>
    )
}

export default Tag;